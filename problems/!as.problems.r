# as.problems

# TRIED:
if (!isGeneric("as.lines")) { setGeneric("as.lines", function(x,...) standardGeneric("as.lines")) }

# TRYING:
setGeneric("as.lines", function(x, ...) standardGeneric("as.lines"))

# TRY:
As #1 but no curly brackets